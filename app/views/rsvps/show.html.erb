<p id="notice"><%= notice %></p>
<%= @rsvp.event.name %>
<%= @rsvp.event.date %>
<%= link_to 'Back', events_path(@event) %>
<%= link_to "Cancel my RSVP", 
    rsvp_path(@rsvp), 
    data: { confirm: "Are you sure?" }, 
    method: :delete,
    class: "button is-primary media-right is-pulled-right" %>

